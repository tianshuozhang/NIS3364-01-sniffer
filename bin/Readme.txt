1.基于python文件生成可执行文件，这里通过pyinstaller实现。当前文件夹下其他子文件夹及文件均是通过pyinstaller自动生成。

3.最终生成的可执行文件为\dist文件夹下packet.exe文件

4.运行平台为Windows平台

5.测试方法可以打开exe文件依次进行网卡选择，过滤设置，抓包，日志存储读取以及IP统计等其他功能。

6.程序保存日志后会在\dist文件夹下创建\txts和\pcaps两个文件夹。

